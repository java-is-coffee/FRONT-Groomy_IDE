import styles from "../../../styles/webIDE/sideContainer/sideContainer.module.css";

const Chat = () => {
  return (
    <div className={styles["side-content"]}>
      <div className={styles["side-container-header"]}>
        <span className={styles["side-container-title"]}>CHAT</span>
      </div>
    </div>
  );
};

export default Chat;
